// This is a test file to see the write tool panel rendering
// It should show a box-drawing border with a "Write" badge

export function greet(name: string, greeting: string = "Hello"): string {
  return `${greeting}, ${name}! Welcome to the meron suite.`;
}

export function add(a: number, b: number): number {
  return a + b;
}

export const PI_VERSION = "1.3.0";
